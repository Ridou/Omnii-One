---
description: 
globs: 
alwaysApply: false
---
# OMNII Profile Screen Implementation Plan
## "Make AI work like YOU work"

### Executive Overview
Transform the basic profile screen into a comprehensive **Personal Productivity DNA Hub** that seamlessly integrates with OMNII's gamification system, mascot evolution, and AI personality tuning. This implementation merges cutting-edge UX patterns from Shape of AI with OMNII's existing design system to create an intuitive, powerful personalization experience.

### Design System Integration
- **Color Palette**: Full `AppColors` system from `constants/Colors.ts`
- **Animation Framework**: Reuse shimmer, pulse, and scale patterns from approvals screen
- **Typography**: Consistent with existing `TYPOGRAPHY_PRESETS` system
- **Component Design**: 16px border radius, same shadow system, `AppColors.cardBackground`
- **AI Theme**: Gradient system (`AppColors.aiGradientStart` → `AppColors.aiGradientEnd`)
- **Navigation**: Filter tabs with animated shimmer effects (identical to approvals screen)

---

## 🧬 SECTION 1: PERSONAL PRODUCTIVITY DNA

### 1.1 Work Style Assessment Component
**File**: `components/profile/WorkStyleAssessment.tsx`

```typescript
interface WorkStyleAssessmentProps {
  onComplete: (results: ProductivityDNA) => void;
  currentProgress?: number;
  onXPAwarded?: (amount: number, reason: string) => void;
}

interface ProductivityDNA {
  workStyle: 'analytical' | 'creative' | 'collaborative' | 'independent';
  energyPattern: 'morning' | 'afternoon' | 'evening' | 'flexible';
  communicationStyle: 'direct' | 'detailed' | 'visual' | 'conversational';
  goalOrientation: 'achievement' | 'growth' | 'impact' | 'balance';
  workEnvironment: 'home' | 'office' | 'hybrid' | 'nomadic';
  focusPreference: 'deep_work' | 'multi_task' | 'collaborative' | 'flexible';
}
```

**Features (Shape of AI + OMNII Integration)**:
- **Suggestions Pattern**: Pre-filled assessment templates based on common productivity profiles
- **Follow Up Pattern**: Dynamic clarifying questions based on initial responses
- **Personal Voice Tuning**: Results directly influence AI communication style
- **Trust Indicators**: Confidence meter showing AI certainty about recommendations
- **XP Integration**: Award "Self-Discovery (75 XP)" upon completion

**Visual Design**:
- Interactive card-based quiz with approval screen styling
- Animated progress bar with AI gradient colors
- Emoji-enhanced answer options with scale animations
- Real-time preview of how choices affect AI behavior
- Results visualization with personality radar chart
- Shimmer effects on active question cards

### 1.2 Energy Mapping & Peak Performance
**File**: `components/profile/EnergyMapping.tsx`

```typescript
interface EnergyMappingProps {
  onUpdate: (energyData: EnergyData) => void;
  currentData?: EnergyData;
  aiInsights?: string[];
}

interface EnergyData {
  peakHours: number[];
  lowEnergyHours: number[];
  preferredBreakTimes: number[];
  focusSessionLength: number;
  energyTriggers: string[];
  optimalWorkDuration: number;
}
```

**Features (Shape of AI + Gamification)**:
- **Token Layering**: Drag-and-drop energy level tokens onto 24-hour timeline
- **Parameters Pattern**: Set constraints for AI scheduling (no meetings during low energy)
- **References Pattern**: AI uses energy data to anchor all scheduling decisions
- **Regenerate Option**: "Try different energy patterns" button
- **XP Triggers**: Track energy optimization achievements

**Visual Design**:
- Interactive 24-hour heatmap with gradient visualization
- Draggable energy tokens with haptic feedback
- Real-time AI suggestions based on energy mapping
- Achievement progress indicators for energy optimization
- Same card elevation and shadow system as approvals

### 1.3 Communication Preferences Hub
**File**: `components/profile/CommunicationHub.tsx`

```typescript
interface CommunicationHubProps {
  onUpdate: (preferences: CommunicationPrefs) => void;
  currentPreferences?: CommunicationPrefs;
}

interface CommunicationPrefs {
  notificationStyle: 'immediate' | 'batched' | 'scheduled' | 'proactive';
  updateFrequency: 'realtime' | 'hourly' | 'daily' | 'weekly';
  preferredChannels: ('push' | 'email' | 'sms' | 'slack')[];
  quietHours: { start: number; end: number };
  urgencyOverride: boolean;
  contextualInterruptions: boolean;
  learningMode: 'aggressive' | 'balanced' | 'conservative';
}
```

**Features (Enhanced with Shape of AI)**:
- **Nudges Pattern**: Smart suggestions for optimal notification timing
- **Filters Pattern**: Constrain notifications by type, urgency, source
- **Memory Pattern**: AI remembers your response patterns to improve timing
- **Caveat Indicators**: Clear warnings about notification impacts
- **Sample Response**: Preview how different settings affect daily workflow

**Visual Design**:
- Toggle switches with AI gradient when active (matching approvals filter tabs)
- Visual clock interface for quiet hours with animation
- Channel preference cards with connection status indicators
- Real-time notification preview with sample scenarios
- Settings impact visualization with before/after comparisons

### 1.4 Goal Hierarchy & Values System
**File**: `components/profile/GoalHierarchy.tsx`

```typescript
interface GoalHierarchyProps {
  onUpdate: (hierarchy: GoalHierarchy) => void;
  currentHierarchy?: GoalHierarchy;
  mascotLevel?: 'seed' | 'flower' | 'tree';
}

interface GoalHierarchy {
  primaryGoals: Goal[];
  secondaryGoals: Goal[];
  coreValues: string[];
  timeHorizons: { daily: Goal[], weekly: Goal[], monthly: Goal[], yearly: Goal[] };
  impactMetrics: string[];
  balancePreferences: { work: number, life: number, growth: number, health: number };
}
```

**Features (Gamification + AI Integration)**:
- **Drag & Drop Ordering**: Visual goal prioritization with spring animations
- **Primary Sources**: Goals feed directly into AI task generation
- **Workflow Integration**: Goals automatically influence approval suggestions
- **Milestone Tracking**: Visual progress toward goal achievement
- **Mascot Integration**: Tree-level users can mentor others on goal setting

**Visual Design**:
- Draggable goal cards with elevation changes during drag
- Priority indicators using approval screen priority colors
- Goal category badges with themed gradients
- Progress visualization with XP-style progress bars
- Value sliders with real-time AI behavior preview

---

## 🤖 SECTION 2: AI PERSONALITY TUNING

### 2.1 Coaching Style & AI Persona
**File**: `components/profile/AIPersonaTuner.tsx`

```typescript
interface AIPersonaTunerProps {
  onUpdate: (persona: AIPersona) => void;
  currentPersona?: AIPersona;
}

interface AIPersona {
  coachingStyle: 'gentle' | 'firm' | 'motivational' | 'analytical' | 'adaptive';
  proactivityLevel: number; // 0-100 slider
  decisionAuthority: 'suggest' | 'decide_low_risk' | 'decide_medium_risk' | 'full_autonomy';
  languageTone: 'professional' | 'casual' | 'motivational' | 'friendly' | 'adaptive';
  personalityTraits: string[];
  learningAggressiveness: 'conservative' | 'balanced' | 'aggressive';
}
```

**Features (Shape of AI Core Patterns)**:
- **Personal Voice**: Fine-tune AI communication to match your style
- **Parameters**: Set constraints for AI autonomy and decision-making
- **Regenerate**: "Try different AI personality" with A/B comparison
- **Show the Work**: Preview AI reasoning process for transparency
- **Token Transparency**: See exactly how AI processes your preferences

**Visual Design**:
- Coaching style cards with character illustrations and sample interactions
- Proactivity slider with gradient track and real-time behavior examples
- Decision authority progressive disclosure with risk level indicators
- Voice preview with text-to-speech sample interactions
- AI reasoning transparency panel with step-by-step logic

### 2.2 Notification Intelligence & Timing
**File**: `components/profile/NotificationIntelligence.tsx`

```typescript
interface NotificationIntelligenceProps {
  onUpdate: (settings: IntelligentNotifications) => void;
  currentSettings?: IntelligentNotifications;
}

interface IntelligentNotifications {
  contextAwareness: boolean;
  energyLevelDetection: boolean;
  calendarIntegration: boolean;
  locationAwareness: boolean;
  batchingRules: BatchingRule[];
  urgencyOverride: UrgencySettings;
  learningFromFeedback: boolean;
  predictiveScheduling: boolean;
}
```

**Features (Advanced Shape of AI)**:
- **Adaptive Frequency**: AI learns optimal notification cadence
- **Controls**: Pause/resume AI notifications mid-stream
- **Footprints**: See AI's decision path for each notification
- **Memory Management**: Control what AI remembers about your response patterns
- **Governors**: Maintain control over AI notification behavior

**Visual Design**:
- Smart timing timeline with AI confidence indicators
- Contextual settings cards with real-world scenario examples
- Learning feedback visualization showing AI improvement over time
- Override controls with clear authority boundaries
- Notification impact preview with productivity metrics

### 2.3 AI Learning Preferences & Boundaries
**File**: `components/profile/AILearningControls.tsx`

```typescript
interface AILearningControlsProps {
  onUpdate: (settings: LearningSettings) => void;
  currentSettings?: LearningSettings;
}

interface LearningSettings {
  dataSourcesEnabled: DataSource[];
  learningSpeed: 'slow' | 'medium' | 'fast';
  privacyLevel: 'minimal' | 'balanced' | 'full';
  feedbackSensitivity: number;
  behaviorAdaptation: boolean;
  crossPlatformLearning: boolean;
  anonymousInsights: boolean;
}
```

**Features (Trust & Privacy Focus)**:
- **Memory Controls**: Granular control over what AI learns and remembers
- **Data Ownership**: Clear control over personal data usage
- **Incognito Mode**: Interact with AI without leaving learning traces
- **Caveats**: Clear warnings about learning implications
- **Watermarks**: Identify AI-influenced vs. user-driven decisions

**Visual Design**:
- Privacy-first controls with lock icons and security indicators
- Learning speed visualization with impact predictions
- Data source toggle cards with detailed explanations
- Privacy impact meter with real-time feedback
- Learning history with option to delete specific interactions

---

## 🔗 SECTION 3: CONNECTION & INTEGRATION HUB

### 3.1 Account Integration Matrix
**File**: `components/profile/IntegrationMatrix.tsx`

```typescript
interface IntegrationMatrixProps {
  onConnect: (service: IntegrationService) => void;
  onConfigure: (service: IntegrationService, config: ServiceConfig) => void;
  connectedServices: IntegrationService[];
}

interface IntegrationService {
  id: string;
  name: string;
  category: 'calendar' | 'email' | 'project' | 'communication' | 'storage';
  status: 'connected' | 'disconnected' | 'error' | 'syncing';
  permissions: Permission[];
  lastSync: Date;
  dataFlow: 'read' | 'write' | 'bidirectional';
  aiIntegration: boolean;
}
```

**Features (Enhanced Integration)**:
- **One-tap OAuth**: Streamlined connection process with progress indicators
- **Service Health Monitoring**: Real-time sync status with error diagnostics
- **Permission Granularity**: Fine-grained control over data access
- **AI Data Flow**: Clear visualization of how integrations feed AI insights
- **Milestone Tracking**: "Integration Master (125 XP)" progress

**Visual Design**:
- Service grid matching approval screen stats layout
- Connection status badges with appropriate colors and animations
- OAuth flow with branded service cards and secure connection indicators
- Data flow visualization showing AI integration points
- Sync health dashboard with performance metrics

### 3.2 Team Synchronization & Collaboration
**File**: `components/profile/TeamSync.tsx`

```typescript
interface TeamSyncProps {
  onUpdate: (settings: TeamSyncSettings) => void;
  currentSettings?: TeamSyncSettings;
  teamMembers?: TeamMember[];
}

interface TeamSyncSettings {
  shareProductivityInsights: boolean;
  anonymizePersonalData: boolean;
  crossTeamLearning: boolean;
  mentorMode: boolean; // Tree-level feature
  collaborativeGoals: boolean;
  teamNudges: boolean;
  leaderboardParticipation: boolean;
}
```

**Features (Community & Growth)**:
- **Team Player Achievement**: Track progress toward "Team Player (200 XP)"
- **Mentor Mode**: Tree-level users can share expertise (Tree Stage benefit)
- **Anonymized Insights**: Privacy-first team collaboration
- **Setup Sage Path**: Help colleagues optimize their profiles for 250 XP
- **Community Milestones**: Contribute to group achievements

**Visual Design**:
- Team member avatars with sharing status indicators
- Privacy controls with detailed anonymization preview
- Mentor mode dashboard with guidance templates
- Achievement sharing interface with social media integration
- Team insights visualization with anonymized data

### 3.3 Privacy & Security Dashboard
**File**: `components/profile/PrivacyDashboard.tsx`

```typescript
interface PrivacyDashboardProps {
  onUpdate: (settings: PrivacySettings) => void;
  currentSettings?: PrivacySettings;
}

interface PrivacySettings {
  dataRetentionPeriod: number;
  personalDataSharing: 'none' | 'anonymized' | 'aggregated';
  crossPlatformSync: boolean;
  localDataProcessing: boolean;
  exportDataOption: boolean;
  deleteAccountOption: boolean;
  auditTrail: boolean;
  biometricProtection: boolean;
}
```

**Features (Privacy Guardian Achievement)**:
- **Data Export**: Complete profile and AI learning data download
- **Retention Controls**: Automatic data cleanup with user-defined periods
- **Privacy Impact Visualization**: Real-time feedback on privacy choices
- **Guardian Achievement**: "Privacy Guardian (100 XP)" tracking
- **Audit Trail**: Complete history of privacy setting changes

**Visual Design**:
- Privacy controls with detailed impact explanations
- Data retention timeline with visual cleanup indicators
- Export functionality with download progress and security verification
- Privacy score meter with recommendations for improvement
- Audit trail with chronological privacy decision history

---

## 🎯 SECTION 4: PERSONALIZATION & CUSTOMIZATION

### 4.1 Mascot Evolution & Customization
**File**: `components/profile/MascotEvolution.tsx`

```typescript
interface MascotEvolutionProps {
  currentLevel: number;
  currentXP: number;
  mascotStage: 'seed' | 'flower' | 'tree';
  onCustomize: (customization: MascotCustomization) => void;
}

interface MascotCustomization {
  name: string;
  primaryColor: string;
  secondaryColor: string;
  personality: 'encouraging' | 'analytical' | 'playful' | 'wise';
  evolutionStyle: 'classic' | 'modern' | 'minimalist' | 'vibrant';
  celebrationStyle: 'subtle' | 'moderate' | 'enthusiastic';
}
```

**Features (Gamification Core)**:
- **Evolution Timeline**: Visual journey from seed → flower → tree
- **Stage Benefits**: Unlock new features at each evolution
- **Growth Prediction**: "At your current pace, you'll bloom in 12 days!"
- **Personality Traits**: Mascot behavior affects AI interaction style
- **Achievement Integration**: Mascot celebrates milestone completions

**Visual Design**:
- Large animated mascot display with level progress
- Customization palette with real-time preview
- Evolution timeline with past stages and future predictions
- Stage benefits showcase with feature unlock animations
- Celebration animation previews with confetti and sound options

### 4.2 Custom Workflows & Templates
**File**: `components/profile/WorkflowBuilder.tsx`

```typescript
interface WorkflowBuilderProps {
  onSave: (workflow: CustomWorkflow) => void;
  existingWorkflows: CustomWorkflow[];
}

interface CustomWorkflow {
  id: string;
  name: string;
  triggers: WorkflowTrigger[];
  actions: WorkflowAction[];
  conditions: WorkflowCondition[];
  frequency: 'daily' | 'weekly' | 'monthly' | 'situational';
  aiLearning: boolean;
}
```

**Features (Advanced Personalization)**:
- **Templates Pattern**: Pre-built workflow templates for common scenarios
- **Madlibs Approach**: Fill-in-the-blank workflow creation
- **Workflow Testing**: Preview mode to test workflow logic
- **AI Enhancement**: Workflows improve with AI learning
- **Personalization Pro**: Track progress toward 150 XP achievement

**Visual Design**:
- Drag-and-drop workflow builder with visual connections
- Template library with categorized workflow examples
- Testing interface with step-by-step execution preview
- AI enhancement indicators showing learning improvements
- Share workflow templates with team members

### 4.3 Theme & Accessibility Controls
**File**: `components/profile/ThemeAccessibility.tsx`

```typescript
interface ThemeAccessibilityProps {
  onUpdate: (settings: ThemeSettings) => void;
  currentSettings?: ThemeSettings;
}

interface ThemeSettings {
  colorScheme: 'light' | 'dark' | 'auto' | 'high_contrast';
  fontSize: 'small' | 'medium' | 'large' | 'xl';
  reducedMotion: boolean;
  voiceControls: boolean;
  screenReaderOptimized: boolean;
  hapticFeedback: 'none' | 'light' | 'medium' | 'strong';
  colorBlindSupport: boolean;
}
```

**Features (Inclusive Design)**:
- **Progressive Disclosure**: Advanced accessibility options hidden initially
- **Real-time Preview**: See theme changes immediately across interface
- **Voice Control Integration**: Hands-free profile management
- **Smart Defaults**: AI-suggested settings based on usage patterns
- **Accessibility Audit**: Built-in compliance checking

**Visual Design**:
- Theme preview cards with live interface examples
- Accessibility controls with clear impact descriptions
- Voice control setup with training and testing interface
- Smart recommendations based on detected user needs
- Compliance indicators with accessibility standard references

---

## 🏆 SECTION 5: MILESTONES & ACHIEVEMENT SYSTEM

### 5.1 Profile Milestone Tracker
**File**: `components/profile/MilestoneTracker.tsx`

```typescript
interface MilestoneTrackerProps {
  milestones: ProfileMilestone[];
  currentXP: number;
  onMilestoneComplete: (milestone: ProfileMilestone) => void;
}

interface ProfileMilestone {
  id: string;
  title: string;
  description: string;
  xpReward: number;
  progress: number;
  maxProgress: number;
  completed: boolean;
  category: 'discovery' | 'integration' | 'privacy' | 'team' | 'personalization';
  unlocksAt?: 'seed' | 'flower' | 'tree';
}
```

**Milestone Categories**:
- **Self-Discovery (75 XP)**: Complete full work style assessment
- **Integration Master (125 XP)**: Connect 5+ productivity tools successfully
- **Privacy Guardian (100 XP)**: Thoughtfully customize all privacy settings
- **Team Player (200 XP)**: Share anonymized productivity insights with colleagues
- **Personalization Pro (150 XP)**: Customize AI personality and workflow preferences
- **Setup Sage (250 XP)**: Help 3 colleagues optimize their OMNII profiles

**Visual Design**:
- Milestone cards with gradient progress bars and XP counters
- Category organization with themed colors and icons
- Completion celebrations with confetti animations and mascot reactions
- Progress tracking with detailed completion steps
- Social sharing options for milestone achievements

### 5.2 XP System Integration
**File**: `hooks/useProfileXP.ts`

```typescript
interface ProfileXPHook {
  currentXP: number;
  level: number;
  mascotStage: 'seed' | 'flower' | 'tree';
  nextLevelXP: number;
  milestones: ProfileMilestone[];
  awardXP: (amount: number, reason: string, category: string) => void;
  checkMilestones: () => void;
  getUnlockedFeatures: () => string[];
}
```

**Features**:
- **Real-time XP Tracking**: Instant feedback for profile completion actions
- **Level Progression**: Visual progression toward next mascot evolution
- **Feature Unlocks**: New capabilities at flower and tree stages
- **Achievement Notifications**: Celebration animations for milestone completion
- **Progress Persistence**: Cross-device synchronization of achievement data

---

## 📱 MAIN PROFILE SCREEN LAYOUT

### Header Section
```typescript
// Identical to approvals screen structure
// - Title: "Profile" with AI gradient
// - Subtitle: "Make AI work like YOU work"
// - User avatar with XP level badge and mascot stage indicator
// - AIStatusBadge component (reused from approvals)
```

### Navigation Tabs (Identical to Approvals)
```typescript
// Same shimmer-effect filter tabs as approvals screen
const profileTabs = [
  { key: 'dna', label: 'DNA', gradient: [AppColors.aiGradientStart, AppColors.aiGradientEnd] },
  { key: 'ai', label: 'AI Tuning', gradient: ['#667eea', '#764ba2'] },
  { key: 'connect', label: 'Connect', gradient: ['#4ECDC4', '#44A08D'] },
  { key: 'milestones', label: 'Progress', gradient: ['#FFB347', '#FFD700'] }
];
```

### Content Sections
- **Scrollable sections** with identical card styling to approvals
- **Progressive disclosure** for advanced settings
- **Contextual help** with tooltip system
- **Real-time preview** of AI behavior changes
- **Achievement progress** visible throughout interface

---

## 🛠 TECHNICAL IMPLEMENTATION

### State Management Architecture
```typescript
// ProfileContext for centralized state management
interface ProfileContextValue {
  productivityDNA: ProductivityDNA;
  aiPersona: AIPersona;
  integrations: IntegrationService[];
  privacy: PrivacySettings;
  milestones: ProfileMilestone[];
  updateDNA: (dna: Partial<ProductivityDNA>) => void;
  updateAIPersona: (persona: Partial<AIPersona>) => void;
  awardXP: (amount: number, reason: string) => void;
}
```

### Data Persistence Strategy
- **Local First**: AsyncStorage for immediate access
- **Background Sync**: Supabase for cross-device consistency
- **Offline Support**: Queue changes when offline, sync when connected
- **Privacy Compliance**: Local data processing where possible
- **Backup/Export**: Complete profile data export functionality

### Animation System (Reused from Approvals)
```typescript
// Identical animation patterns from approvals screen
// - Shimmer effects for loading states and active tabs
// - Scale animations for card interactions (0.98 scale on press)
// - Pulse animations for status indicators
// - Gradient animations for AI elements
// - Spring animations for drag-and-drop reordering
```

### Performance Optimizations
- **Lazy Loading**: Heavy components load on demand
- **React.memo**: Prevent unnecessary re-renders
- **useCallback**: Optimize event handlers
- **Image Optimization**: Compressed mascot and icon assets
- **Bundle Splitting**: Profile-specific code in separate bundle

---

## 🎨 ENHANCED DESIGN SYSTEM

### Animation Library
```typescript
export const ProfileAnimations = {
  // Reuse all approvals screen animations
  ...Animations,
  
  // Profile-specific additions
  mascotEvolution: {
    duration: 2000,
    useNativeDriver: true,
  },
  xpCounter: {
    duration: 800,
    useNativeDriver: false, // For number interpolation
  },
  settingsReveal: {
    duration: 300,
    useNativeDriver: true,
  }
};
```

### Component Hierarchy
```typescript
ProfileScreen
├── ProfileHeader (with XP badge)
├── ProfileTabs (identical to approvals tabs)
├── TabContent
│   ├── ProductivityDNASection
│   │   ├── WorkStyleAssessment
│   │   ├── EnergyMapping
│   │   ├── CommunicationHub
│   │   └── GoalHierarchy
│   ├── AITuningSection
│   │   ├── AIPersonaTuner
│   │   ├── NotificationIntelligence
│   │   └── AILearningControls
│   ├── ConnectionSection
│   │   ├── IntegrationMatrix
│   │   ├── TeamSync
│   │   └── PrivacyDashboard
│   └── ProgressSection
│       ├── MascotEvolution
│       ├── MilestoneTracker
│       └── AchievementGallery
```

---

## 🚀 IMPLEMENTATION ROADMAP

### Phase 1: Foundation (Week 1-2)
- **Core State Management**: ProfileContext and data models
- **Basic UI Structure**: Header, tabs, and navigation (reusing approvals patterns)
- **Work Style Assessment**: Complete quiz component with XP integration
- **Mascot Display**: Basic evolution visualization

### Phase 2: AI Tuning (Week 3-4)
- **AI Persona Controls**: Coaching style, proactivity, decision authority
- **Notification Intelligence**: Context-aware timing and batching
- **Learning Controls**: Privacy-first AI training preferences
- **Real-time Preview**: Show AI behavior changes immediately

### Phase 3: Integrations (Week 5-6)
- **Account Integration**: OAuth flows for major productivity tools
- **Team Synchronization**: Privacy-first collaboration features
- **Privacy Dashboard**: Complete data control and export functionality
- **Integration Milestones**: Track and reward connection achievements

### Phase 4: Advanced Features (Week 7-8)
- **Custom Workflows**: Drag-and-drop workflow builder
- **Theme System**: Complete theming and accessibility controls
- **Mascot Customization**: Full personalization with stage-based unlocks
- **Achievement System**: Complete milestone tracking and rewards

### Phase 5: Polish & Optimization (Week 9-10)
- **Performance Optimization**: Lazy loading, memoization, bundle splitting
- **Animation Refinement**: Smooth 60fps animations throughout
- **Accessibility Audit**: Full compliance with accessibility standards
- **User Testing**: Feedback collection and iterative improvements

---

## 📊 SUCCESS METRICS & KPIs

### User Engagement Metrics
- **Profile Completion Rate**: % of users completing each section
- **Feature Usage**: Analytics for each personalization feature
- **Time Investment**: Average time spent in profile sections
- **Milestone Velocity**: Speed of achievement completion
- **Return Frequency**: How often users revisit profile settings

### AI Effectiveness Metrics
- **Task Completion Improvement**: Before/after profile optimization
- **Notification Satisfaction**: User feedback on AI timing and content
- **Workflow Efficiency**: Time saved through personalized AI behavior
- **Learning Accuracy**: AI prediction improvement over time
- **User Satisfaction**: NPS scores for AI personalization

### Technical Performance Metrics
- **Load Times**: All screens load under 2 seconds
- **Animation Performance**: Consistent 60fps throughout interface
- **Memory Usage**: Optimized for device capabilities
- **Battery Impact**: Minimal background processing impact
- **Crash Rate**: Zero tolerance for profile-related crashes

---

This comprehensive implementation plan creates a world-class profile experience that seamlessly integrates Shape of AI UX patterns with OMNII's gamification system, maintains complete visual consistency with the approvals screen, and provides users with unprecedented control over their AI productivity partner. The modular architecture allows for incremental development while ensuring each feature enhances the overall user experience and contributes to the app's core mission of evolving with users as they grow in their productivity journey.