[test]
# Configure bun test settings
preload = ["./tests/setup.js"]
timeout = 15000 # 15 seconds - increased timeout for MCP API calls
# Retry failed tests 2 times
retries = 2

# ESM compatibility
jsx = "automatic"
logLevel = "info"