# MCP Backend Configuration
# Copy to .env.local for local development (gitignored)
#
# IMPORTANT: Most infrastructure variables should be set in ROOT .env
# Only MCP-specific overrides belong here.

# ============================================================
# PHASE 2 REQUIRED VARIABLES - Graph Core & MCP Server
# ============================================================

# === Shared Infrastructure (OMNII_* namespace) ===

# Supabase (required for auth and user database tracking)
OMNII_SUPABASE_URL=https://your-project.supabase.co
OMNII_SUPABASE_ANON_KEY=your-anon-key
OMNII_SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Neo4j Aura (required for database provisioning - Plan 01-04)
OMNII_NEO4J_AURA_API_TOKEN=your-aura-api-token
OMNII_NEO4J_AURA_TENANT_ID=your-tenant-id

# OpenAI (required for embeddings - Plan 02-02)
OMNII_OPENAI_API_KEY=sk-your-openai-key

# Redis (optional - for distributed rate limiting in production)
# OMNII_REDIS_URL=redis://localhost:6379

# === MCP App-Specific ===
MCP_BASE_URL=http://localhost:8081
MCP_PORT=8081

# === Admin (optional - for manual schema setup endpoint) ===
# ADMIN_KEY=your-secret-admin-key

# === Environment ===
NODE_ENV=development

# ============================================================
# LEGACY / OPTIONAL VARIABLES
# ============================================================

# === Legacy Neo4j (dev only, production uses per-user DBs) ===
# For local development with shared Neo4j instance
# NEO4J_URI=neo4j+s://your-instance.databases.neo4j.io
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=your-password
# NEO4J_DATABASE=neo4j

# === n8n Agent Integration ===
N8N_AGENT_SWARM_URL=https://santino62.app.n8n.cloud
N8N_AGENT_ENABLED=false
N8N_AGENT_TIMEOUT=60000

# === Third-party APIs (MCP-specific) ===
COMPOSIO_API_KEY=your-composio-api-key
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# === Twilio (SMS Integration) ===
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890
TWILIO_TEST_PHONE_NUMBER=+1234567890
