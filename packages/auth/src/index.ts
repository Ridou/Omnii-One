// Export Supabase client factories and types
export * from './supabase';

// Export auth middleware (created in Task 2)
export * from './middleware';

// Legacy Session type export for backward compatibility
// (Session is re-exported from supabase.ts)
